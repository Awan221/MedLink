from django.db.models.signals import post_save
from django.dispatch import receiver
from django.conf import settings

# Les signaux seront implémentés ici
# Cette structure vide est suffisante pour le moment pour éviter les erreurs d'import
